#!/usr/bin/python

import sys
import subprocess

msg = " ".join(sys.argv[1:])
escaped = msg.replace("\\", "\\\\").replace('"', '\\\"')
arg = '(stumpwm-message "{}")'.format(escaped)
subprocess.call(["emacsclient", "-e", arg])

# Local Variables:
# mode: python
# End:
